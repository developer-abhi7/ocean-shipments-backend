const note_ctrl = require('./note.controller');
const container_ctrl = require('./container.controller');
const shipment_ctrl = require('./shipment.controller');
module.exports = {
    note_ctrl,
    container_ctrl,
    shipment_ctrl
};
